@page
@model Storeii.Pages.SuborderItems.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Suborder_Items[0].Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Suborder_Items[0].SubOrderId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Suborder_Items[0].Product_Id)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Suborder_Items) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SubOrderId.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Product_Id)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
