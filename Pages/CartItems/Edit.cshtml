@page
@model Storeii.Pages.CartItems.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>CartItem</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="CartItem.Id" />
            <div class="form-group">
                <label asp-for="CartItem.Customer_Id" class="control-label"></label>
                <select asp-for="CartItem.Customer_Id" class="form-control" asp-items="ViewBag.Customer_Id"></select>
                <span asp-validation-for="CartItem.Customer_Id" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CartItem.Product_Id" class="control-label"></label>
                <select asp-for="CartItem.Product_Id" class="form-control" asp-items="ViewBag.Product_Id"></select>
                <span asp-validation-for="CartItem.Product_Id" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
